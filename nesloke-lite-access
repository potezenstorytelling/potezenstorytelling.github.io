<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="robots" content="noindex, nofollow">
<title>Nesloke Lite: Your Free Downloads</title>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;900&family=Crimson+Text:ital,wght@0,400;0,600;1,400;1,600&display=swap" rel="stylesheet">
<style>
  :root {
    --gold: #C9A84C;
    --gold-light: #E8C96A;
    --gold-dim: rgba(201,168,76,0.15);
    --neon: #39FF14;
    --neon-dim: rgba(57,255,20,0.08);
    --dark: #0A0A0F;
    --dark-mid: #111118;
    --dark-card: #13131C;
    --text: #D4C9A8;
    --text-muted: #8A7F6A;
    --white: #F0EAD6;
  }
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body {
    font-family: "Crimson Text", serif;
    background: var(--dark);
    color: var(--text);
    font-size: 19px;
    line-height: 1.75;
    overflow-x: hidden;
    min-height: 100vh;
  }
  .particles {
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    overflow: hidden;
    pointer-events: none;
    z-index: 1;
  }
  .particle {
    position: absolute;
    border-radius: 50%;
    animation: float linear infinite;
    opacity: 0;
  }
  @keyframes float {
    0% { transform: translateY(100vh) scale(0); opacity: 0; }
    10% { opacity: 1; }
    90% { opacity: 0.6; }
    100% { transform: translateY(-20px) scale(1); opacity: 0; }
  }

  .wrap {
    position: relative;
    z-index: 3;
    max-width: 760px;
    margin: 0 auto;
    padding: 90px 32px 60px;
  }

  .eyebrow {
    font-family: "Cinzel", serif;
    font-size: 0.8rem;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--neon);
    text-align: center;
    margin-bottom: 20px;
  }

  h1 {
    font-family: "Cinzel", serif;
    font-size: clamp(2.2rem, 5vw, 3.4rem);
    font-weight: 900;
    color: var(--gold);
    text-align: center;
    margin-bottom: 20px;
    text-shadow: 0 0 40px rgba(201,168,76,0.3);
    line-height: 1.2;
  }

  .intro {
    text-align: center;
    color: var(--text);
    max-width: 620px;
    margin: 0 auto 8px;
  }

  .ornament {
    display: flex;
    align-items: center;
    gap: 16px;
    justify-content: center;
    margin: 40px auto;
    max-width: 400px;
  }
  .ornament-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }
  .ornament-diamond {
    width: 8px; height: 8px;
    background: var(--gold);
    transform: rotate(45deg);
    flex-shrink: 0;
  }

  .downloads {
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin: 0 auto;
    max-width: 620px;
  }

  .dl-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    background: var(--dark-card);
    border: 1px solid var(--gold-dim);
    border-radius: 4px;
    padding: 24px 28px;
    transition: border-color 0.3s, transform 0.3s;
  }
  .dl-card:hover {
    border-color: var(--gold);
    transform: translateY(-2px);
  }
  .dl-text { flex: 1; }
  .dl-title {
    font-family: "Cinzel", serif;
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--gold-light);
    margin-bottom: 4px;
  }
  .dl-desc {
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.5;
  }

  .dl-btn {
    font-family: "Cinzel", serif;
    font-size: 0.8rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--dark);
    background: var(--gold);
    border: 1px solid var(--gold);
    border-radius: 3px;
    padding: 12px 22px;
    text-decoration: none;
    white-space: nowrap;
    transition: background 0.3s, color 0.3s, box-shadow 0.3s;
    flex-shrink: 0;
  }
  .dl-btn:hover {
    background: var(--gold-light);
    box-shadow: 0 0 24px rgba(201,168,76,0.35);
  }

  .kicker {
    text-align: center;
    max-width: 600px;
    margin: 56px auto 0;
    padding: 28px 32px;
    background: var(--neon-dim);
    border: 1px solid rgba(57,255,20,0.18);
    border-radius: 4px;
  }
  .kicker-title {
    font-family: "Cinzel", serif;
    color: var(--neon);
    font-size: 1.1rem;
    letter-spacing: 1px;
    margin-bottom: 10px;
  }
  .kicker p { color: var(--text); font-size: 1rem; }
  .kicker a {
    color: var(--gold-light);
    font-weight: 600;
  }

  footer {
    position: relative;
    z-index: 3;
    background: var(--dark);
    border-top: 1px solid rgba(201,168,76,0.15);
    padding: 32px 32px 40px;
    text-align: center;
    color: var(--text-muted);
    font-size: 0.85rem;
    margin-top: 60px;
  }
  footer a { color: var(--gold); text-decoration: none; }

  @media (max-width: 560px) {
    .dl-card { flex-direction: column; align-items: stretch; text-align: center; }
    .dl-btn { width: 100%; text-align: center; }
  }
</style>
</head>
<body>
<div class="particles" id="particles"></div>

<div class="wrap">
  <div class="eyebrow">Welcome, Traveler</div>
  <h1>Your Nesloke Lite Downloads</h1>
  <p class="intro">Everything you need to make a character and play through your first levels of Nesloke is right here. Download the three files below, gather a group, and begin.</p>

  <div class="ornament">
    <div class="ornament-line"></div>
    <div class="ornament-diamond"></div>
    <div class="ornament-line"></div>
  </div>

  <div class="downloads">
    <div class="dl-card">
      <div class="dl-text">
        <div class="dl-title">Player's Guide</div>
        <div class="dl-desc">The core system, combat, five archetypes, character creation, and how you grow.</div>
      </div>
      <a class="dl-btn" href="/Nesloke-Lite%20Players%20Guide.pdf" download>Download</a>
    </div>

    <div class="dl-card">
      <div class="dl-text">
        <div class="dl-title">GM Guide</div>
        <div class="dl-desc">Everything your Game Master needs to run the game, including the mini-campaign.</div>
      </div>
      <a class="dl-btn" href="/Nesloke%20Lite%20GM%20Guide.pdf" download>Download</a>
    </div>

    <div class="dl-card">
      <div class="dl-text">
        <div class="dl-title">Character Sheet</div>
        <div class="dl-desc">A print-ready sheet to track your attributes, skills, conditions, and gear.</div>
      </div>
      <a class="dl-btn" href="/Nesloke-Lite%20Character%20Sheet.pdf" download>Download</a>
    </div>
  </div>

  <div class="kicker">
    <div class="kicker-title">The Kickstarter Is Coming</div>
    <p>If Nesloke Lite pulls you in, the full game launches on Kickstarter soon: all twelve archetypes, deeper lore, crafting, and more. <a href="https://www.kickstarter.com/projects/kevtyler/nesloke-ttrpg">See the campaign</a>.</p>
  </div>
</div>

<footer>
  Potezen Storytelling LLC &middot; <a href="https://potezen.com">potezen.com</a>
</footer>

<script>
  const container = document.getElementById("particles");
  const colors = ["#39FF14", "#C9A84C", "#7FFF00", "#ADFF2F", "#39FF14", "#39FF14"];
  function createParticles() {
    const pageHeight = document.body.scrollHeight;
    const count = 60;
    for (let i = 0; i < count; i++) {
      const p = document.createElement("div");
      p.classList.add("particle");
      const size = Math.random() * 4 + 1;
      const color = colors[Math.floor(Math.random() * colors.length)];
      const startY = Math.random() * pageHeight;
      const duration = Math.random() * 20 + 12;
      const delay = Math.random() * 20;
      p.style.cssText = `
        width: ${size}px;
        height: ${size}px;
        left: ${Math.random() * 100}%;
        top: ${startY}px;
        background: ${color};
        animation-duration: ${duration}s;
        animation-delay: ${delay}s;
        box-shadow: 0 0 ${size * 2}px ${color};
      `;
      container.appendChild(p);
    }
  }
  window.addEventListener("load", createParticles);
</script>
</body>
</html>
